services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: haylog_frontend
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env.production
